from django.http import HttpResponse
from django.shortcuts import render_to_response
from vutman.models import EmailUser


def index(request):
    user_list = EmailUser.objects.all()
    return render_to_response("index.html",
                              {
                                  'user_list': user_list
                              }
    )
